@require './general'

html {
    box-sizing: border-box
    font-family: $font-family
    font-size: $font-size
    background: $base-background-color
    line-height: 1.2
    margin: 0 auto
}

a {
    text-decoration: none

    &:link, &:visited {
        color: $white
    }
}